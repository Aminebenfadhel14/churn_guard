# ============================================================
# ChurnGuard — Dépendances Python (Python 3.11+)
# Installation :  pip install -r requirements.txt
# ============================================================

# ----- API REST -----
fastapi>=0.115,<1.0
uvicorn[standard]>=0.30,<1.0
python-multipart>=0.0.9        # upload de fichiers (datasets) via l'API

# ----- Données -----
pandas>=2.2,<3.0
numpy>=1.26,<2.2
openpyxl>=3.1,<4.0             # lecture/écriture Excel (.xlsx)
pyarrow>=16.0                  # lecture/écriture Parquet
charset-normalizer>=3.3        # détection automatique de l'encodage (ingestion)

# ----- Machine Learning -----
scikit-learn>=1.5,<2.0
xgboost>=2.1,<3.0
lightgbm>=4.4,<5.0
imbalanced-learn>=0.12,<1.0    # SMOTE

# ----- IA explicable -----
shap>=0.46,<1.0
catboost>=1.2            # gradient boosting (optionnel, gere nativement le categoriel)

# ----- MLOps -----
mlflow>=2.14,<3.0
joblib>=1.4,<2.0

# ----- Configuration & clients HTTP -----
python-dotenv>=1.0,<2.0
httpx>=0.27,<1.0               # appels HTTP (couche MCP -> API FastAPI)
pydantic-settings>=2.3,<3.0    # config typée via variables d'environnement

# ----- Couche MCP -----
fastmcp>=2.0,<3.0

# ----- Tests -----
pytest>=8.2,<9.0
pytest-cov>=5.0,<6.0           # mesure de la couverture de tests
